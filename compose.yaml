services:
  basics:
    image: housebuilder
    container_name: housebuilder
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      TZ: "Australia/Brisbane"
      NODE_ENV: development
    ports:
      - 3000:3000